type User @model {
  id: ID!
  email: String!
  confirmedDate: String!
  forms: [Form!]
}

type Form {
  id: ID!
  contents: FormContents!
} 

type FormContents {
  columns: Int
  rows: Int
  elements: [FormElement!]
}

type FormElement {
  type: String
  start: Int
  end: Int
}