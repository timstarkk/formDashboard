type User @model {
  id: ID!
  email: String!
  confirmedDate: String!
  forms: [Form!]
}

type Form {
  id: ID!
  contents: [FormContents!]
} 

type FormContents {
  columns: Int
  rows: Int
  layout: [Row!]
}

type Row {
  something: String
}